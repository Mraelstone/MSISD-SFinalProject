<?php

$someVar = 5;

function foo() {
  return 'bar';
}

// echo to print
echo $somevar

// print out 'bar'
echo foo();

class MyClass
{
  public $someVar
  public function addOne($) {
    return $n + 1;
  }
}

$inst = new MyClass();

// Dollar sign only at very front (not $someVar) but still says whole
// thing is a variable
// arrow is like concatenate
$inst->someVar = 5

$temp = 'addOne'

$arr = [];
// arrays: in PHP arrays and dictionaries are the same thing
// in javascript and python arrays and dictionaries are two seperate things

// adds something to the end of an array two options:
array_push($arr, 'a');
$arr[] = 'b'

// programming debugger like log
var_dump($arr)

// associated array - has operated key values, they are ordered,
// will always print out in the same order
$arr['key']='val';
